# 2026-07-15 Flutter Bug 修复终审

对照基线：`D:\Work\learn\photo-album` 微信小程序；审查对象为当前 Flutter 工作区最终代码，而不是历史提交说明。

今天后续的冲突处理曾回退部分 UI，微信登录按钮、首页设备卡、我的设备操作栏等问题确实重新出现过。本轮已按当前较新的微信授权和后端逻辑逐项合并，未用旧提交整文件覆盖。

| # | 项目 | 最终状态 |
|---|---|---|
| 1 | 微信授权入口圆形图标、置底 | 已修复，并增加 48×48 回归测试 |
| 2 | 三处投屏按钮统一拍照/相册弹层 | 已核对，统一复用 `CastPhotoPicker.chooseSource` |
| 3 | 投屏完成“返回首页”按钮 | 已核对，使用白色胶囊次按钮 |
| 4 | 投屏管理加载空态闪烁 | 已核对，列表接口返回前展示页内 loading |
| 5 | 首页/我的 Tab 图标 48rpx | 已核对，Flutter 尺寸均为 24×24 |
| 6 | 首页 device-carousel 本地背景/阴影/毛玻璃 | 已修复，使用本地 `home-bg01.png`、24 圆角、0/4/16 阴影、10.55 blur |
| 7 | 我的设备页面样式 | 已恢复小程序操作栏样式和整按钮点击区 |
| 8 | 详情页清空/删除右侧文案宽度 | 已核对，右侧最大宽度 193px，不再使用过小固定宽度 |
| 9 | 切换语种页面不更新 | 已修复并持久化，增加当前页即时中译英测试 |
| 10 | 图库选中后的底部操作区 | 已核对，刷新/删除/取消为小程序同款选择操作栏 |
| 11 | 图库刷新指定图片索引错误 | 已修复，按上传先后映射升序固件槽位，并兼容缺设备 ID 的旧数据 |
| 12 | 投屏记录再次/重新投屏 | 已修复，先下载预览图并重新进入裁剪；编辑后上传最后保存图片 |
| 13 | 上传/删除后的设备内存错误 | 已修复，操作后重试回读 0x01；刷新后端列表时保留 BLE 实时字段 |
| 14 | 轮播设置 UI | 已核对，小程序同款开关、提示、顺序/随机单选及禁用态 |
| 15 | 删除当前屏显照片后的刷新 | 已核对，命中当前索引时从 0 起取删除后首个有效槽位刷新 |
| 16 | APP 启动页 | 已增加 Flutter 启动遮罩和 Android 原生启动背景，使用 `logo.png` + `bg01.png` |
| 17 | 删除设备二次确认 | 已核对并补齐第一步断开设备、第二步确认删除 |
| 18 | 我的设备空态 | 已核对，包含空态插画、文案和添加设备按钮 |
| 19 | 立即绑定延迟/loading 提前消失 | 已修复，loading 保持到判重/后端绑定完成，移除额外等待后立即返回 |
| 20 | 设备列表/详情投屏与连接点击区 | 已修复，点击区域覆盖整个按钮 |
| 21 | 手机桌面 APP 图标 | 已使用 `assets/images/logoV2.png` 重新生成 Android/iOS 图标资源 |

验证：`dart analyze lib test` 无问题；`flutter test --no-pub` 全部通过；Android profile 分 ABI 构建通过。
