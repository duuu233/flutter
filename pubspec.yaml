name: BoltStar
description: 'A new Flutter project.'
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter
  # Material/Cupertino 内建组件文案（文本选择菜单/无障碍标签等）的官方本地化，
  # 配合 MaterialApp 的 localizationsDelegates + locale 跟随 App 语言切换。
  flutter_localizations:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  # 网络图片内存+磁盘双层缓存：图库/投屏记录/头像。纯 Image.network 只有内存缓存，
  # 冷启动或缓存逐出后全量重新下载（一页 100 张 ≈ 20~40MB 流量、数秒白图）。
  cached_network_image: ^3.4.1
  # cached_network_image 的底层磁盘缓存管理器。本来是它的传递依赖，这里**显式提升为直接依赖**：
  # 退出登录 / 注销 / 登录态失效时要清掉上个账号的图片磁盘缓存（隐私，见 ImageCacheCleanup），
  # 而 `DefaultCacheManager` 只能从这个包导入。直接 import 传递依赖会在上游改版本时突然断裂。
  flutter_cache_manager: ^3.4.1
  # 蓝牙图传：扫描/连接/MTU/收发/notify，且自带 requestConnectionPriority(图传前后调连接间隔)
  flutter_blue_plus: ^2.3.8
  # BoltFox 后端接口请求（lib/src/network）：JSON 请求与 form-data 文件上传
  http: ^1.2.2
  # http 的传递依赖，这里显式提升为直接依赖（同 flutter_cache_manager 的理由）：
  # 抖动接口 multipart 文件字段要显式标 Content-Type: image/jpeg（MediaType 只能从这里导入）
  http_parser: ^4.0.2
  # 密码等敏感字段 md5(32位小写) 加密后传输（对齐小程序 utils/md5.js）
  crypto: ^3.0.6
  # 拍照/相册选图，返回真实本地文件路径（投屏原图需上传后端转码，content:// 无法直接上传）
  image_picker: ^1.1.2
  # Android 相册统一使用系统 Photo Picker；显式依赖平台实现与接口，以便在 main() 启用。
  # Photo Picker 只授权用户选中的图片，不需要 READ_MEDIA_IMAGES / READ_EXTERNAL_STORAGE。
  image_picker_android: ^0.8.13+17
  image_picker_platform_interface: ^2.11.1
  # 版本更新：跳转浏览器/应用商店下载新版 APK
  url_launcher: ^6.3.0
  # 读取当前 App 版本号，供 getLastVersion 比对是否有新版本
  package_info_plus: ^10.2.1
  # 本地持久化（登录邮箱历史缓存等轻量键值）
  shared_preferences: ^2.3.2
  # 微信开放平台移动应用授权；业务层通过适配器隔离，便于后续替换 SDK
  fluwx: ^6.0.0
  # 投屏预览页的「裁剪」：**原生**裁剪器（Android=uCrop / iOS=TOCropViewController）。
  # 小程序是自己用 canvas + touch 事件手搓的裁剪框（783 行），那是被微信沙箱逼的；
  # App 直接用原生裁剪器：手势更跟手、自带旋转/网格/回弹，还能锁定宽高比并直接导出 JPEG。
  # ⚠️ Android 需要在 AndroidManifest.xml 注册 UCropActivity（已加）。
  image_cropper: ^12.2.1
  # AI 聊天「按住说话」的端上语音识别（iOS SFSpeechRecognizer / Android SpeechRecognizer）。
  # 口径见接口文档 v1.0.3 §5.1.3：识别在**手机本地**做，拿到文字直接走 POST /chat，
  # 音频不出手机、**后端零改动**（小程序那边同理，用微信「同声传译」插件在端上转文字）。
  # ⚠️ 版本写成区间而不是 `^x`：`listen()` 的参数在 6.6 起才收进 `SpeechListenOptions`
  # （旧的 partialResults/cancelOnError 具名参数 7.0 已移除），下限钉在 6.6.0 才两边都能编；
  # 上限到 8.0 之前跟随 pub.dev 最新即可。
  # ⚠️ 安卓还需要清单里的 RECORD_AUDIO + `<queries>` 里的 RecognitionService（都已加），
  #   iOS 需要 NSMicrophoneUsageDescription + NSSpeechRecognitionUsageDescription（已加）。
  speech_to_text: '>=6.6.0 <8.0.0'
  # 程序化图片处理（**不参与交互**）：只用于两件事——
  #   ① 工具栏「旋转」按钮的即时 90° 旋转（走原生裁剪器要开一整个页面，太重）；
  #   ② 用户没做任何编辑时，投屏前按设备比例做一次 aspectFill 中心裁切。
  # 必须能编码 JPEG：dart:ui 的 toByteData 只支持 png，而 png 是同画质 jpg 的 5~10 倍体积，
  # 直接拖慢投屏的上传阶段（小程序也为此从 png 换成了 jpg q0.92）。
  image: 4.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
    - assets/
    - assets/images/
    - assets/images/BatteryLevel/

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
